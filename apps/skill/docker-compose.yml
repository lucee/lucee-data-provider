services:
  lucee:
    build: .
    image: lucee-skill:latest
    environment:
      LUCEE_ADMIN_PASSWORD: "change-me"
    volumes:
      - "./www:/var/www"
    ports:
      - "8055:8080" # PORT (Cloud Run compatible)

# docker compose up -d
